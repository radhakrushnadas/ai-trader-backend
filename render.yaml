services:
  - type: web
    name: ai-trader-backend
    env: python
    pythonVersion: 3.11.8       # Use Python 3.11 to avoid pandas compile errors
    plan: free
    region: oregon
    buildCommand: pip install --upgrade pip && pip install -r requirements.txt
    startCommand: python main.py
    autoDeploy: true
